[project]
name = "f1-chart"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "requests",
    "matplotlib",
    "pandas",
    "plotly>=6.2.0",
    "pycountry>=24.6.1",
    "coverage>=7.9.2",
]
